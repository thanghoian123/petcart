import React from 'react';
import PropTypes from 'prop-types';
import Store from './shop/store';

Shop.propTypes = {
    
};

function Shop(props) {
    return (
        <div>
            <Store/>
        </div>
    );
}

export default Shop;